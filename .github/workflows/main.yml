name: Secrets Check - Commit
  
on: [push]

jobs:
  security-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Trufflehog Actions Scan
        uses: Plabick/trufflehog-actions-scan@master
