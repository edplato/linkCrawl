name: Secrets Check - Commit
  
on: [push]

jobs:
  security-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Trufflehog Actions Scan test
        uses: edplato/trufflehog-actions-scan@debug-rules
        with:
          scanArguments: "--regex --entropy=False --max_depth=100 --rules /regexes.json"
