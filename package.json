{
  "name": "linkCrawl",
  "version": "1.0.0",
  "description": "Use Chromium headless browser to test for dead links.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "EAP",
  "license": "ISC",
  "dependencies": {
    "cli-spinner": "^0.2.8",
    "puppeteer": "^1.3.0"
  }
}
